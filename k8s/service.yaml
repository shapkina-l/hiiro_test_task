apiVersion: v1
kind: Service
metadata:
  name: devops-task
  namespace: devops-task
spec:
  selector:
    app: devops-task
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP